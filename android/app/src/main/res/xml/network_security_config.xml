<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Android cannot express private CIDR ranges here, and the app connects to arbitrary LAN IPs over ws://. -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
